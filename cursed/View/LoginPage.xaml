<Page x:Class="cursed.View.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:cursed.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Логин: "></TextBlock>
                <TextBox Name="TBoxLogin" Width="200"></TextBox>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Пароль:"></TextBlock>
                <PasswordBox Name="PBoxPassword" Width="200"></PasswordBox>
            </StackPanel>
            <Button Name="BtnLogin" Background="#7ed6df" Click="BtnLogin_Click" Content="Войти" HorizontalAlignment="Right" Width="200" Margin="0 10 0 0"></Button>
            <Button Name="BtnRegister" Background="Transparent" Click="BtnRegister_Click" Content="Регистрация" HorizontalAlignment="Right" Margin="0 10 0 0"/>
        </StackPanel>
    </Grid>
</Page>
